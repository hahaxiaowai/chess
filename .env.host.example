# Copy this file to `.env.host` before running the host-Nginx deployment script.
# `APP_PORT` is bound to 127.0.0.1 only, so the app stays behind the host Nginx reverse proxy.
APP_IMAGE=chess-app:latest
APP_PORT=3101
