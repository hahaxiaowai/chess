{
  "name": "chess",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm run --filter chess dev",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  }
}
