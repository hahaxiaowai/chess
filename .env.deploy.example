# Copy this file to `.env` before running `docker compose up -d`.
# Build locally with `docker build -t chess-app:latest .`, or point to a pushed registry image.
APP_IMAGE=chess-app:latest

# Expose Nginx on the host port you want to serve publicly.
HTTP_PORT=80
